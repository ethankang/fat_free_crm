- if (params['controller'] == 'leads') and (template = template_for_current_view).present?
  = render(template, :lead => lead)
- elsif params[:type].present?
  = render 'leads/index_long', :lead => lead
- else
  -# default view
  = render 'leads/index_brief', :lead => lead
